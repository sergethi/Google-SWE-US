import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//ReactDOM.render(param1, param2)
//param1 -> what react component?
//param2 -> location where you want your React component to render into
ReactDOM.render(
    <App /> //our react component
,
  document.getElementById('root')
);


